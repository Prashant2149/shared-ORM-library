{
  "name": "shared-orm-library",
  "version": "1.0.0",
  "description": "Shared ORM library for database entities and connection",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nestjs/typeorm": "^11.0.0",
    "dotenv": "^16.4.7",
    "pg": "^8.14.1",
    "typeorm": "^0.3.21"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.9.0"
  }
}
